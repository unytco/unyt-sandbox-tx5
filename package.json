{
  "name": "unyt-shipyard",
  "version": "0.0.0",
  "private": true,
  "license": "CAL-1.0",
  "workspaces": [
    "unyt",
    "unyt/ui/lit-2"
  ],
  "scripts": {
    "network:tauri": "concurrently \"cd unyt && yarn network:tauri\" \"yarn launch\"",
    "launch": "concurrently \"yarn tauri dev --no-watch\" \"yarn tauri dev --no-watch\"",
    "launch:android": "yarn tauri android dev",
    "tauri": "tauri"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "concurrently-repeat": "^0.0.1",
    "@tauri-apps/cli": "^2.0.0",
    "get-port-cli": "3.0.0"
  },
  "resolutions": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
